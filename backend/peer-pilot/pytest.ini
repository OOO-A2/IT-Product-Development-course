[pytest]
minversion = 8.0
testpaths = tests
python_files = test_*.py

addopts =
    -q
    --cov=app.api.endpoints.dashboard
    --cov=app.api.endpoints.grades
    --cov=app.api.endpoints.peer_reviews
    --cov-report=xml:coverage.xml
    --cov-report=term-missing
    --cov-fail-under=30
